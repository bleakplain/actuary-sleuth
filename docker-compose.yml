services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - scripts/.env
    volumes:
      - app-data:/app/scripts/data
      - ./references:/app/references:ro

volumes:
  app-data:
